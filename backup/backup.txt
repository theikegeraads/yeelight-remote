# self.ipField = qtw.QLineEdit()
        # self.ipField.setPlaceholderText("Lamp IP address...")
        # self.ipField.setStyleSheet("background-color: white;")
        # container.layout().addWidget(self.ipField, 5, 0)

        # self.ipLabel = qtw.QLabel("192.168.178.11")
        # self.ipLabel.setStyleSheet("color: white;")
        # container.layout().addWidget(self.ipLabel, 0, 1)

        # connectButton = qtw.QPushButton("Connect", clicked = lconnect)
        # connectButton.setStyleSheet("QPushButton { background-color: #2D2D2D; color: white; }")
        # container.layout().addWidget(connectButton, 7, 0)

        # toggleButton = qtw.QPushButton("Power", clicked = lToggle)
        # toggleButton.setMaximumWidth(45)
        # toggleButton.setMinimumHeight(42)
        # toggleButton.setStyleSheet("QPushButton { color: #FDFDFD; background: red }")
        # container.layout().addWidget(toggleButton, 0, 0)

        # onButton = qtw.QPushButton("On", clicked = lToggle)
        # onButton.setStyleSheet("QPushButton { color: white; background: #2D2D2d }")
        # container.layout().addWidget(onButton, 1, 0)

        # offButton = qtw.QPushButton("Off", clicked = lOff)
        # offButton.setStyleSheet("QPushButton { background-color: #2D2D2D; color: white; }")
        # container.layout().addWidget(offButton, 2, 0)

        # redButton = qtw.QPushButton("Red")
        # redButton.setStyleSheet("QPushButton { background-color: #2D2D2D; color: white; }")
        # container.layout().addWidget(redButton, 5, 0)
      
        # greenButton = qtw.QPushButton("Green")
        # greenButton.setStyleSheet("QPushButton { background-color: #2D2D2D; color: white; }")
        # container.layout().addWidget(greenButton, 5, 1)
        
        # blueButton = qtw.QPushButton("Blue")
        # blueButton.setStyleSheet("QPushButton { background-color: #2D2D2D; color: white; }")
        # container.layout().addWidget(blueButton, 5, 2)


        # brightDial = qtw.QDial(valueChanged = lBrightness)
        # brightDial.setMinimum(0)
        # brightDial.setMaximum(100)
        # brightDial.setStyleSheet("QDial { Background-color: #2D2D2D; }")
        # container.layout().addWidget(brightDial, 3, 1)

        # brightLabel = qtw.QLabel("Brightness: ")
        # brightLabel.setStyleSheet("QLabel { color: white; }")
        # container.layout().addWidget(brightLabel, 3, 0)

        # rSlider = qtw.QSlider(Qt.Horizontal)
        # rSlider.setMinimum(0)
        # rSlider.setMaximum(255)
        # rSlider.setTickInterval(1)
        # rSlider.setSingleStep(1)
        # rSlider.setStyleSheet("QSlider::add-page:horizontal { background-color: red; }")
        # rSlider.setStyleSheet("QSlider::sub-page:horizontal { background-color: blue; }")
        # container.layout().addWidget(rSlider, 5, 5)